

<div class="container">
  <% @articles.each do |article| %>
  <%= link_to article_path(article) do %>
  <img src="<%= article.photo_url %>" alt="">

  <%= article.title %>
  <%= article.subtitle %>
  <%= article.writer %>
  <%end  %>
<%end  %>
</div>
